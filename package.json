{
  "name": "chime-trifin-playground",
  "scripts": {
    "dev": "serverless offline ",
    "debug": "set SLS_DEBUG=* && node --inspect node_modules/serverless/bin/serverless offline -s stage",
    "dynamo:start": "serverless dynamodb start",
    "deploy": "serverless deploy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.102",
    "@types/uuid": "^8.3.4",
    "esbuild": "^0.14.25",
    "serverless-esbuild": "^1.25.0",
    "serverless-offline": "^9.2.6"
  },
  "dependencies": {
    "aws-embedded-metrics": "^3.0.1",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1203.0",
    "serverless": "^3.22.0"
  }
}
